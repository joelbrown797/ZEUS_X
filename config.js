module.exports = {
  owner: process.env.OWNER_NUMBER || '+22800000000',
  botName: process.env.BOT_NAME || 'ZEUS_X',
  sessionFile: process.env.SESSION_FILE || 'session.json',
  prefix: '!',  // préfixe pour les commandes
}

